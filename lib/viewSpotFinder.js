'use strict';
const _ = require('underscore');

module.exports = {
  viewSpotFinder: (meshObject, numberOfViewSpots) => {
  
    console.log(meshObject)
    console.log(numberOfViewSpots)
    return true
  }
}

